# Needs a recent SDK for messages.parse() + output_format and claude-opus-4-8.
# If `parse` is missing at runtime, `pip install -U anthropic`.
anthropic>=0.69
requests>=2.31
python-dotenv>=1.0
feedparser>=6.0
google-api-python-client>=2.0
google-auth>=2.0
google-auth-oauthlib>=1.0
flask>=3.0
gunicorn>=21.0
