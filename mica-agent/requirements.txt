# Runtime deps. Dev/CI tooling lives in requirements-dev.txt.
# A recent anthropic SDK is needed for tool use with claude-sonnet-4-6.
Django>=5.1
djangorestframework>=3.15
anthropic>=0.69
python-dotenv>=1.0
dj-database-url>=2.1
# Async tool calls + rate limits (wired in config; used from P2/P6 onward).
celery>=5.4
redis>=5.0
# Postgres driver for the docker/production stack; SQLite is used for local dev.
psycopg[binary]>=3.2
gunicorn>=21.0
